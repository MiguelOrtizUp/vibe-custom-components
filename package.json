{
  "name": "vibe-custom-components",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/runner & npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/runner && npm run build --workspace=apps/web",
    "install:all": "npm install"
  }
}
